# Procfile para Railway / Heroku
# Railway detecta automaticamente, mas Procfile garante o comando correto.
web: . /opt/venv/bin/activate && cd backend && uvicorn server:app --host 0.0.0.0 --port $PORT
